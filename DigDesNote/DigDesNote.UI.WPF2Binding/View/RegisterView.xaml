<UserControl x:Class="DigDesNote.UI.WPF2Binding.View.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DigDesNote.UI.WPF2Binding.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBox Text="{Binding Login, Mode=TwoWay}"></TextBox>
        <TextBox Text="{Binding Email, Mode=TwoWay}"></TextBox>
        <PasswordBox x:Name="_password"></PasswordBox>
        <Button Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=_password}">Регистрация</Button>
    </StackPanel>
</UserControl>
