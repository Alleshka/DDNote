<UserControl
    x:Class="DigDesNote.UI.WPF2Binding.View.NoteInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigDesNote.UI.WPF2Binding.View"
        xmlns:note="clr-namespace:DigDesNote.UI.WPF2Binding.ViewModel"
        mc:Ignorable="d"
        Height="300" Width="300">
    <StackPanel>
        <StackPanel DataContext="{Binding CurNoteClone, Mode=TwoWay}">
            <TextBox Text="{Binding Path=_title}"/>
            <TextBox Text="{Binding Path=_content}"/>
            <TextBlock Text="{Binding Path=_created}"/>
            <TextBox Text="{Binding Path=_updated}"/>
        </StackPanel>
        <Button Content="Сохранить" Command="{Binding UpdateNoteCommand}"/>
        <Button Content="Отмена" Command="{Binding CanselCommand}"/>
        <Button Content="Категории" Command="{Binding OpenViewCategoryCommand}"/>
        <Button Content="Пользователи" Command="{Binding ViewShareListCommand}"/>
    </StackPanel>
</UserControl>
