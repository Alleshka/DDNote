<UserControl x:Class="DigDesNote.UI.WPF2Binding.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <StackPanel>
        <TextBox Text="{Binding Login, Mode=TwoWay}" TextAlignment="Center"></TextBox>
        <PasswordBox x:Name="_password"></PasswordBox>
        <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=_password}">Вход</Button>
        <Button Command="{Binding RegisterCommand}">Регистрация</Button>
    </StackPanel>
</UserControl>
